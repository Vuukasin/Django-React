from django.urls import path
from .views import *

urlpatterns = [

    # path('api/user/', UserListView.as_view()),
    # path('api/user/<str:username>/', UserDetailView.as_view()),
    # path('api/home/profile/', HomePageProfile.as_view()),

]